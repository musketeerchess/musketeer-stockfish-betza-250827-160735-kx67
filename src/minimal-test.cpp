// Minimal test compilation to debug issues
#include <iostream>

// Test if basic includes work
#include "types.h"

// Test basic function
int main() {
    std::cout << "Minimal test compilation successful!" << std::endl;
    return 0;
}